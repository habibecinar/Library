{
  "name": "habibe-goit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build --base=/Library/",
    "preview": "vite preview",
    "goLive": "npm run dev",
    "goPreview": "npm run build && npm run preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "simplelightbox": "^2.14.3"
  }
}
